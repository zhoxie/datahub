namespace com.linkedin.datasource

/**
 * Fine Grained upstream lineage for fields in a datasource
 */
@Aspect = {
  "name": "datasourceUpstreamLineage"
}
record DatasourceUpstreamLineage {
  /**
   * Upstream to downstream field level lineage mappings
   */
  fieldMappings: array[DatasourceFieldMapping]
}